import { useLanguage } from "@/contexts/LanguageContext"; import { MapPin, Truck } from "lucide-react";
export default function ServiceAreasSection(){const{t}=useLanguage();return <section id="serviceAreas" className="service-new"><div className="network-map"><div className="network-orbit a"/><div className="network-orbit b"/><div className="network-route"/><div className="network-hub"><Truck/></div>{t.serviceAreas.areas.map((a:string,i:number)=><span key={a} className={`network-pin pin-${i%6}`}><i/><b>{a}</b></span>)}</div><div className="service-copy"><span className="section-label">07 / DELIVERY NETWORK</span><h2>Close to<br/>where <i>business happens.</i></h2><p>{t.serviceAreas.subtitle}</p><div><MapPin/> {t.serviceAreas.footer}</div></div></section>}
