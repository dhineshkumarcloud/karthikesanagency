import { useLanguage } from "@/contexts/LanguageContext"; import { ArrowUpRight } from "lucide-react";
export default function BrandsSection(){const{t}=useLanguage();return <section id="brands" className="brands-new"><header><span className="section-label">06 / BRAND WALL</span><h2>Names your<br/>customers <i>know.</i></h2></header><div className="brand-wall">{t.brands.list.map((b:string,i:number)=><div key={b} className={`brand-${i%5}`}><span>0{i+1}</span>{b}</div>)}</div><div className="partner-line"><p>{t.partnership.title} — {t.partnership.description}</p><button onClick={()=>document.getElementById("contact")?.scrollIntoView({behavior:"smooth"})}>{t.partnership.cta}<ArrowUpRight/></button></div></section>}
